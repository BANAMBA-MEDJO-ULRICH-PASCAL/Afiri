/**
 * config.js — Configuration de l'URL de l'API Afiri.
 *
 * C'est LE SEUL fichier à modifier avant de déployer.
 *
 * En développement local :
 *   const API_URL = "http://localhost:8000";
 *
 * En production (après déploiement sur Render) :
 *   Remplace la valeur ci-dessous par l'URL de ton service back-end Render.
 *   Ex : const API_URL = "https://afiri-api.onrender.com";
 */
const API_URL = "http://localhost:8000";
